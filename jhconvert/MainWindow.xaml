<Window
    x:Class="jhconvert.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:jhconvert"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d"
    Title="MainWindow">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button Grid.Row="0" Content="Load Excel" Click="LoadExcel_Click" Margin="10"/>
        
        <StackPanel Grid.Row="1" Orientation="Vertical" Margin="10">
            <TextBlock Text="Column Options" FontWeight="Bold" Margin="0,0,0,10"/>
            <ListBox x:Name="ColumnOptionsListBox"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Vertical" Margin="10">
            <TextBlock Text="Preview" FontWeight="Bold" Margin="0,0,0,10"/>
            <controls:DataGrid x:Name="ExcelDataGrid"/>
        </StackPanel>
    </Grid>
</Window>